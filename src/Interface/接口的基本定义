         接口定义：interface   抽象方法和全局常量
            主要目的：一个子类可以实现多个接口，避免子类的单继承
         一个接口可以继承多个接口.

         1.接口不允许去继承父类，所有接口绝对不会是Object的子类，但是实现接口的子类是Object的子类，
           所以接口一定通过Object接收

         2.Object类对象可以接收所以数据类型，包括基本数据类型、类对象、接口对象、数组

         3.接口是一个公共的定义标准，所以接口之中的所有抽象方法的访问权限都为public

         4.一个普通类只能继承一个抽象类，并且可以实现多个接口， ————先继承后实现

         5.JDK1.8 接口可以定义普通方法，需要加default关键字 ，

         6.接口也可以定义static方法，并且可以通过接口直接调用

         **接口需要被子类实现 implements
         **一个子类可以实现多个接口
         **子类如果不是抽象类，需要覆写接口之中的全部抽象方法
         **接口对象可以利用子类对象的向上转型进行实例化