
     基本数据类型进行包装处理后可以像 对象一样进行引用传递，

     基本数据类型八种：提供八种包装类

           Number{abstract} ：Integer、Byte、Long、Short、Float、Double      数值型包装类
Object
           Boolean 、Character    对象型包装类


        包装类相等判断的时候一定要使用equals（）方法完成,包装类需要考虑占位的长度，
        如果超过一位的内容，就需要使用equals比较

        Integer x = 128；
        Integer y = 128；
        Integer z = new Integer(128) ;
        sout(x == y);            //false
        sout(x == z) ;                         //跟String一样
        sout(x.equals(y));     //true
