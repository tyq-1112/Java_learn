线程同步：

    解决线程同步问题的关键是锁：指当某一个线程执行操作的时候，其它线程外面等待

  关键字：synchronized  定义同步方法和同步代码块
         在同步代码块的操作里面的代码只允许一个线程执行

   1.  synchronized(同步对象){
           同步代码操作
     }

     同步对象处理的时候可以采用当前对象this进行同步

     加入同步处理之后，程序的性能下降了，同步实际上会造成性能降低

   2.同步方法：只需要在方法定义上加synchronized